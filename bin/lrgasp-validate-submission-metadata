#!/usr/bin/env python3

import sys
from os import path as osp
import argparse
import logging

sys.path.insert(0, osp.normpath(osp.join(osp.dirname(__file__), "../lib")))

from lrgasp import handle_prog_errors
from lrgasp import LrgaspException
from lrgasp import submission

def parse_args():
    desc = """Validate that an LRGASP submission meets requirements.
    """
    parser = argparse.ArgumentParser(description=desc)
    parser.add_argument("submission_json",
                        help="submission JSON")
    return parser.parse_args()

def main(args):
    logging.getLogger().setLevel(logging.WARN)
    try:
        submission.submission_load(args.submission_json)
    except LrgaspException as ex:
        handle_prog_errors(ex)

main(parse_args())
